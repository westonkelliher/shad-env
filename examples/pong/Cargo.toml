[package]
name = "pong"
version = "0.1.0"
edition = "2021"

[dependencies]
shad-env = { path = "../.." }
winit = "0.29"
pollster = "0.3"
image = { version = "0.25", default-features = false, features = ["png"] }

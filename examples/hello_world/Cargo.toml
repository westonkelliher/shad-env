[package]
name = "hello_world"
version = "0.1.0"
edition = "2021"

[dependencies]
shad-env = { path = "../.." }
winit = "0.29"
pollster = "0.3"
bytemuck = "1"
image = { version = "0.25", default-features = false, features = ["png"] }

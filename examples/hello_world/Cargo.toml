[package]
name = "hello_world"
version = "0.1.0"
edition = "2021"

[dependencies]
windowed-shad-env = { path = "../../windowed-shad-env" }
bytemuck = "1"
image = { version = "0.25", default-features = false, features = ["png"] }
